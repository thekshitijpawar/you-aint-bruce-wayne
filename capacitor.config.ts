import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brucewayne.moneytracker',
  appName: 'Bruce Wayne Tracker',
  webDir: 'dist'
};

export default config;
